@*@{
        ViewBag.Title = "Inex";
    }

    <h2>Inex</h2>*@
@model PagedList.IPagedList<MvcVehicleService4.Models.VehicleModelViewModel>
@using PagedList.Mvc;

@{
    /**/

    ViewBag.Title = "Vehicle Model";
}

<h2>Vehicle Models</h2>
<p>

    @Html.ActionLink("Create New", "Create")

</p>


@using (Html.BeginForm())
{
    <p>
        Find Vehicle Model by MakId: @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)
        <input type="submit" value="Search" />
    </p>
}
<table class="table">
    <thead>
        <tr>
            @*<th>
                    ID
                </th>*@
            <th>
                @Html.ActionLink("ID", "Index", new { Sorting_Order = ViewBag.SortingVehicleModelID, currentFilter = ViewBag.CurrentFilter })
            </th>
            @*<th>
                    name
                </th>*@
            <th>
                @Html.ActionLink("MakeId", "Index", new { Sorting_Order = ViewBag.SortingVehicleModelMakeId , currentFilter = ViewBag.CurrentFilter })
            </th>
            <th>
               
                @Html.ActionLink("Name", "Index", new { Sorting_Order = ViewBag.SortingVehicleModelName ,currentFilter = ViewBag.CurrentFilter })
            </th>
            @*<th>
                    abrv
                </th>*@
            <th>
                @Html.ActionLink("Abrv", "Index", new { Sorting_Order = ViewBag.SortingVehicleModelAbrv ,currentFilter = ViewBag.CurrentFilter })
            </th>

        </tr>
    </thead>
    <tbody>
        @*@if (ViewBag.ListaVMView != null)
            {
                foreach (var item in ViewBag.ListaVMView)
                {
                    <tr>
                        <td>@item.ID</td>
                        <td>@item.Name</td>
                        <td>@item.Abrv</td>
                        <td>
                            @Html.ActionLink("Delete", "Delete", new { id = item.ID }) |
                            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                        </td>
                      </tr>
                }
            }*@
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MakeId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Abrv)
                </td>

                <td>
                    @Html.ActionLink("Delete", "Delete", new { id = item.ID }) |
                    @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                </td>
            </tr>
        }
    </tbody>





    </tbody>
</table>
    @*age @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount*@
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }),
                                                new PagedListRenderOptions() {Display = PagedListDisplayMode.IfNeeded , DisplayPageCountAndCurrentLocation = true } )

   <h4>
        @Html.ActionLink("Vehicle Makes", "Index", "VehicleMake")
    </h4>
